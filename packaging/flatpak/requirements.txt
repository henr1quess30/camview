# Runtime dependencies resolved into python3-requirements.json by
# build.sh. PySide6 is deliberately absent: it comes from the
# io.qt.PySide.BaseApp base, not from PyPI.
python-vlc>=3.0.20123
keyring>=25.0
