# docs

`screenshot-mosaic.png` — the picture referenced by the AppStream
metadata (and therefore by Flathub).

It shows four **synthetic test patterns** served over RTSP from
localhost, not real cameras. That is deliberate: a screenshot of a live
wall would publish footage of real people and reveal how a real site is
covered. Regenerate it the same way rather than pointing the app at
production equipment.
